const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d //operador infix
console.log(soma)

const subtracao = d - d
console.log(subtracao)

const multiplicacao = a * b
console.log(multiplicacao)

const divisao = d / a
console.log(divisao)

const modulo = a % 2
console.log(modulo)

//++soma operador prefix

//soma++ operador posfix

//-soma operador unario

//a + b operador binario